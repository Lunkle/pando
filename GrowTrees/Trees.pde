class Trees{  
  String name;
  int seedDispersal;
  float treeHeight;
  float treeAge;

  Player(String name, int seedDispersal, float treeHeight, float treeAge) {
    this.name = name;
    this.seedDispersal = seedDispersal;
    this.treeHeight = treeHeight;
    this.treeAge = treeAge;
  }
  
}
